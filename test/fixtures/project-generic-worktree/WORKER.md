# Generic Project Worker

Execute the given assignment with clear ownership boundaries.
Keep writes constrained to the explicit worktree path that is embedded in the prompt.
Keep all changes and notes inside the assigned worktree unless explicitly told otherwise.
